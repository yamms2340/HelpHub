name: CI-CD

on:
  push:
    branches: [ jan2 ]
  pull_request:
    branches: [ jan2 ]

jobs:
  server:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: help-platform/server

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test || echo "No tests found"

      # ===============================
      # CD STEP — DEPLOY TO RENDER
      # ===============================
      - name: Deploy server to Render
        if: github.event_name == 'push' && github.ref == 'refs/heads/jan2'
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_SERVER }}"
